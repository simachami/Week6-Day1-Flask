<nav class="navbar navbar-expand-lg bg-body-tertiary mb-4">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Pinch of Yum</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ url_for('main.home_page') }}">Home</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Recipes
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="{{ url_for('main.recipe') }}">Pasta</a></li>
            <li><a class="dropdown-item" href="#">Random</a></li>
          </ul>
          </li>
          {% if current_user.is_authenticated %}
          <li class="nav-item">
          <a class="nav-link active" href="{{ url_for('social.profile', username=current_user.username) }}">My Page</a>
        </li>
          <li class="nav-item">
          <a class="nav-link" href="{{ url_for('auth.log_out') }}">Log out</a>
        </li>
        {%else%}
        <li class="nav-item">
          <a class="nav-link" href="{{ url_for('auth.sign_in') }}">Sign In</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ url_for('auth.sign_up') }}">Sign Up</a>
          {%endif%}
      </ul>
       <form class="d-flex" method="POST" action="{{ url_for('social.user_search') }}">
        {{ g.recipe_search_form.hidden_tag() }}
        {{ g.recipe_search_form.username() }}
        {{ g.recipe_search_form.submit() }}
      </form>
    </div>
  </div>
</nav>